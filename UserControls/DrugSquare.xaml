<UserControl x:Class="ePharm.UserControls.DrugSquare"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="192" d:DesignWidth="192" SizeChanged="OnSizeChanged">

    <!--TODO:
    Если название товара слишком длинное - добавить 3 точки, вместо последний 3 символов
    -->

    <Grid>
        <Border Width="192" Height="192"  CornerRadius="32" Background="{StaticResource Element}" x:Name="DrugBorder"/>
        <Ellipse Width="96" Height="96" Fill="Purple" VerticalAlignment="Top" Margin="0,16,0,0" x:Name="DrugImage"/>
        <StackPanel VerticalAlignment="Bottom" Margin="0,0,0,16">
            <TextBlock Text="Название препарата" x:Name="DrugNameTextBlock" HorizontalAlignment="Center" FontSize="18"/>
            <TextBlock Text="Тип препарата" x:Name="DrugTypeTextBlock" HorizontalAlignment="Center" FontSize="14"/>
        </StackPanel>
    </Grid>
</UserControl>
